import Head from "next/head";
import useFetch from "@/hooks/useFetch";
import CardDesign from "@/components/cardDesign/CardDesign";
import { Container, Row } from "react-bootstrap";

export default function Senior() {
  const { data, loading } = useFetch("/member/Senior");
  return (
    <>
      <Head>
        <title>Cox&apos;s Bazar Student Forum Senior</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="./shreepur.jpeg" />
      </Head>
      <main>
        {loading ? (
          <div className="loadingContainer">
            <img src="./loading.gif" alt="" className="loadingGif" />
          </div>
        ) : (
          <>
            <div className="headerTitle mt-5">
              <h3 class="headerTitleMain">সিনিয়র সদস্য</h3>
            </div>
            {/* Advisor Details */}
            <Container className="mt-4">
              <Row>
                {data?.data?.map((item) => (
                  <CardDesign key={item.serial} item={item} />
                ))}
              </Row>
            </Container>
          </>
        )}
      </main>
    </>
  );
}
